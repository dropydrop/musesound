CONTEXTE :

Je développe un lecteur audio YouTube 100% côté client (pas de serveur, site statique).

Mon problème actuel : mon fallback vers les instances publiques Invidious/Piped ne fonctionne plus car YouTube bloque massivement ces IPs. Je ne peux pas lancer d'instance locale (pas de serveur), et je ne veux pas télécharger les fichiers (pas dlPro). Je veux juste LIRE l'audio en streaming.

J'ai identifié une solution : YoutubeAudioPlayer (diegorochagranado/YoutubeAudioPlayer) qui utilise l'API YouTube IFrame Player officielle en masquant la vidéo (height:0, width:0). C'est 100% client-side et ne dépend d'aucune instance publique.

MISSIONS PRIORITAIRES :

1. Analyse le repo github.com/diegorochagranado/YoutubeAudioPlayer
   - Identifie tous les fichiers nécessaires (HTML, JS, CSS)
   - Extrait le code exact qui initialise le lecteur audio invisible
   - Identifie comment il gère : l'extraction de l'ID vidéo, l'API key (si nécessaire), les URLs supportées

2. Compare avec mediaelement.js (github.com/oliverfoster/mediaelement) en alternative

3. Produis un plan d'intégration :
   - Quels fichiers copier dans mon projet existant ?
   - Quelles modifications pour que ça fonctionne avec mon fallback actuel ?
   - Comment tester localement sans serveur ?

4. Anticipe les limites :
   - Les publicités YouTube ne sont pas bloquables côté client (à documenter dans l'UI)
   - Nécessite une connexion internet pour charger l'API YouTube

CONTRAINTES TECHNIQUES :
- Pas de backend, pas de build step complexe
- Doit fonctionner en ouvrant index.html directement (file://) ou sur GitHub Pages
- Doit rester léger (pas de framework lourd)
- Le fallback actuel (Invidious) doit rester en place pour les vidéos sans pub si jamais une instance fonctionne

FORMAT DE RÉPONSE ATTENDU :

1. Liste exacte des fichiers à récupérer avec leur URL brute (raw.githubusercontent.com)
2. Pour chaque fichier : son rôle et si je dois le copier tel quel ou l'adapter
3. Code d'intégration minimal pour mon projet existant
4. Si tu n'as pas accès en direct au contenu du repo : dis-moi EXACTEMENT quels fichiers consulter (chemins), je ferai un copier-coller manuel du contenu vers toi.

NOTE IMPORTANTE SUR LE COPIE-COLLER MANUEL :

Si tu ne peux pas accéder directement aux repos GitHub ou si leur contenu est volumineux, indique-moi précisément :
- Le chemin du fichier (ex: /src/player.js)
- La ligne de début à ligne de fin approximative
- Je te copierai le contenu ici même, tu l'analyseras et me diras quoi en faire.

Je suis prêt à faire ce travail manuel fichier par fichier si c'est nécessaire. Ne pars pas du principe que tu as un accès illimité aux repos. Sois explicite sur ce que tu veux que je récupère.

RÉPONDS MAINTENANT AVEC CE PLAN.